/*
 * Mock Three has no seed data — the upload store starts empty each run.
 * Uploads are created through the drag-and-drop UI or via POST /api/uploads.
 */

export {}; // keeps TypeScript happy (empty module)
