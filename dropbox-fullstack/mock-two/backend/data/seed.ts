/*
 * Mock Two has no seed data — the link store starts empty each run.
 * Links are created through the UI or via POST /api/links.
 *
 * If you want pre-populated links for faster testing, create them here
 * and import into server.ts to call seedLinks() on startup.
 */

export {}; // keeps TypeScript happy (empty module)
