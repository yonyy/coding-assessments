/*
 * Mock Four seeding happens directly in server.ts (the for loop on startup)
 * so that generateEvent() is available without a circular import.
 *
 * This file is kept for consistency with the other mocks.
 */

export {}; // keeps TypeScript happy (empty module)
